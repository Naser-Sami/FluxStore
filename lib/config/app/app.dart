import 'package:flutter/material.dart';

import 'localization_wrapper.dart';

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const LocalizationWrapper();
  }
}
